Drop support for the Sled crypto store format. The configuration key of `experimentalEncryption.useLegacySledStore` is now ignored, and the crypto store will always use the SQLite format. If an existing SQLite store does not exist on bridge startup, one will be created.
